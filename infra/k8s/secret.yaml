# TEMPLATE — example/placeholder credentials only, safe to commit because
# they are not real secrets. Replace with values from a real secret
# manager (Sealed Secrets, External Secrets, cloud KMS, etc.) before any
# non-local use. `stringData` is used so kubectl/K8s handles the base64
# encoding — nothing here needs to be pre-encoded by hand.
apiVersion: v1
kind: Secret
metadata:
  name: port-poc-secrets
  namespace: port-poc
type: Opaque
stringData:
  MYSQL_USERNAME: "root"
  MYSQL_PASSWORD: "example-not-a-real-password"
  RABBITMQ_USER: "guest"
  RABBITMQ_PASSWORD: "guest"
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "minioadmin"
  API_KEY: "prod-api-key-changeme"
